#include "TextDetection.hpp"
int main()
{

//    //use the camera
//    VideoCapture cap(0);
//    if (!cap.isOpened()) return -1;
//    Mat input;
//    for (;;) {
//    	cap >> input;
//    	textDetection(input);
//    	imshow("text", input);
//    	if (waitKey(30) >= 0) break;
//    }
//    
//    //read the image
////    Mat input = imread("num.jpg", 1);
////     textDetection(input);
////     waitKey(0);*/
//    return 0;
}